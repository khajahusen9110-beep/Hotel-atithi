{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "VITE_SUPABASE_URL",
      "description": "Supabase project URL (e.g. https://xxxxx.supabase.co). Found in Supabase dashboard → Project Settings → API.",
      "requiredAtBoot": false
    },
    {
      "name": "VITE_SUPABASE_ANON_KEY",
      "description": "Supabase anon/public API key. Found in Supabase dashboard → Project Settings → API. This is a public client key, safe for frontend use.",
      "requiredAtBoot": false
    }
  ]
}
